apiVersion: v1
kind: Service
metadata:
  name: nexus
  namespace: {{ include "bootstrap-tenant.nexusNamespace" . }}
  labels:
    app: nexus
  annotations:
    argocd.argoproj.io/sync-wave: "1"
spec:
  selector:
    app: nexus
  ports:
    - name: http
      port: 8081
      targetPort: 8081
